
<h1>投稿情報</h1>
<p><%= notice %></p>
<table>
  </tr>
<% @pictures.each do |picture| %>
  <ul class="list-group">
    <p><%=picture.image %></P>
    <p><%= picture.content %></p>
    <p><%= picture.user.name%></p>
    <li class="list-group-item"><%= link_to '詳細を確認する', picture_path(picture.id) %></li>
    <li class="list-group-item"><%= link_to "編集する", edit_picture_path(picture.id), data: { confirm: '本当に編集していいですか？' }  %></li>
    <li class="list-group-item"><%= link_to "削除する", picture_path(picture.id), method: :delete,data: { confirm: '本当に削除していいですか？' } %></li>
    </ul>
<% end %>
</table>
<%= link_to '新しい投稿する', new_picture_path %>